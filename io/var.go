package io

import (
	"fmt"
	"strings"
)

var (
	fPln        = fmt.Println
	sJoin       = strings.Join
	sSplit      = strings.Split
	sHasPrefix  = strings.HasPrefix
	sTrim       = strings.Trim
	sReplaceAll = strings.ReplaceAll
)
